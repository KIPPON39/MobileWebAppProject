import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.paikanthoe',
  appName: 'ไปกันเธอ',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
